import React from 'react';

export default function ApplicantLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Applicant layout (navbar, sidebar) - placeholder */}
      {children}
    </div>
  );
}
