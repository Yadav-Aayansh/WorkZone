import React from 'react';

export default function HrAdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* HR/Admin layout placeholder */}
      {children}
    </div>
  );
}
