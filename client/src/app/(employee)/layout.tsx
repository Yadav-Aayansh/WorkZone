import React from 'react';

export default function EmployeeLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Employee layout (navbar/sidebar) - placeholder */}
      {children}
    </div>
  );
}
