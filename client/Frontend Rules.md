## 🧩 Tech Stack

**Framework:** Next.js 14 (App Router) + TypeScript
**Package Manager:** pnpm (required)
**UI & Styling:** Tailwind CSS + shadcn/ui
**Animation:** Framer Motion (light), GSAP (section reveals), Lenis (smooth scroll)
**Forms & Validation:** React Hook Form + Zod
**Data Fetching:** native fetch + (optional) React Query for server state
**Auth (planned):** NextAuth.js (OAuth/JWT)
**Icons:** Lucide
**Deployment:** Vercel (Root Dir: `client`, Build: `pnpm build`)

---

## 🌿 Branching & PR Workflow (Frontend)

**Base Branch:** `frontend` (for UI team)
**Feature Branches:**

* `feature/sandesh-landing-page`
* `feature/abhishek-login-ui`

**PR Flow:**

1. Devs → PR into `frontend`
2. Frontend Lead (Sandesh) → PR `frontend` → `develop` after review & QA

**Commit Format (Conventional Commits):**

```
feat:    new feature added
fix:     bug fix
chore:   config or dependency changes
refactor: code refactoring
style:   formatting or styling
perf:    performance improvements
docs:    documentation
```

---

## 🧱 shadcn/ui Rules

* All design-system components live in `src/components/ui/*` (auto-generated by shadcn).
* **Do not edit shadcn primitives inline.** Extend via composition using wrappers.
* Custom wrapper naming convention → `FormField`, `PrimaryButton`, etc.
* Prefer **Server Components** by default; use **Client Components** only when necessary (`"use client"`).

---

## 🎨 Styling Rules (Tailwind + CSS Variables)

**Core Principle:** No hardcoded colors (use Tailwind CSS variables only).

**Color Management:**

* Define all tokens in `globals.css` and map them in `tailwind.config.ts`.
* Follow **shadcn color semantics** for consistency.

**Allowed Classes:**

```tsx
bg-background  text-foreground  border-border
bg-primary      text-muted-foreground
```

❌ Avoid direct hex values or Tailwind raw colors (e.g., `text-[#1e90ff]`, `bg-red-500`).

**Spacing & Typography:**
Use Tailwind scales (`p-6`, `text-sm`, etc.)
**Dark Mode:** class strategy using `ThemeProvider` and variable sets in `:root` / `.dark`.

---

## 🧩 Components & Pages

* Keep components **pure and presentational**.
* Side effects → handled in **hooks**.
* Co-locate styles next to the component file or use Tailwind utilities.

**Structure Guidelines:**

* `components/sections/*` → Hero, Features, CTA, etc.
* `components/applicant/*`, `employee/*`, `hr-admin/*` → dashboard widgets.

---

## 🔗 Data & API

* All fetchers in `lib/api/*`
* Base URL → `NEXT_PUBLIC_API_URL` (from `.env.local`)
* Add basic error & loading states
* Prefer server-side `fetch` with caching when suitable

---

## ✅ PR Checklist (Frontend)

* [ ] Follows folder structure and naming convention
* [ ] Uses **pnpm**
* [ ] Uses **Tailwind CSS variables** (no hardcoded colors)
* [ ] Uses **shadcn components** from `components/ui/*`
* [ ] Mobile-first & fully responsive
* [ ] Basic accessibility (labels, alt, focus)
* [ ] No console or TypeScript errors

---

## ⚙️ Environments

Create `/client/.env.local` file:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=WorkZone.tech
```

---

## 👥 Task Division

| Member       | Responsibility                                            |
| ------------ | --------------------------------------------------------- |
| **Sandesh**  | Main Landing Page, Product-side Frontend                  |
| **Abhishek** | Client Landing Page, Applicant Portal, Employee Dashboard |
